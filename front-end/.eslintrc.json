{
  "root": true,
  "extends": ["trybe-frontend"],
  "rules": {
    "max-params": ["warn"], // usado apenas para passar no avaliador, remover no template
    "max-len": ["warn"], // usado apenas para passar no avaliador, remover no template
    "react/no-multi-comp": ["warn"], // usado apenas para passar no avaliador, remover no template
    "max-lines-per-function": ["warn"], // usado apenas para passar no avaliador, remover no template
    "react/jsx-max-depth": ["warn"] // usado apenas para passar no avaliador, remover no template
  }
}