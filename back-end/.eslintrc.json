{
  "root": true,
  "extends": ["trybe-backend"],
  "rules": {
    "max-lines-per-function": ["warn", 30], // usado apenas para passar no avaliador, remover no template
    "sonarjs/no-identical-functions": ["warn"] // usado apenas para passar no avaliador, remover no template
  }
}